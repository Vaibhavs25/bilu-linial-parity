# Release snapshot

This frozen repository snapshot contains:
- audited verification code and drivers;
- audited data artifacts;
- the exact final integrated LaTeX source at submission/final_combined_paper_final.tex;
- the exact SHA-256 manifest at submission/SHA256SUMS.txt.

The final 22-page PDF is distributed separately in the release package because the available GitHub connector cannot upload binary release assets. Its exact SHA-256 is recorded in the manifest.

The corrected campaign protocol has not been rerun after the final manuscript edits; no new campaign numbers are claimed.
